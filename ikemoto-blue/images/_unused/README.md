# Unused images

These image files are **not referenced** anywhere in the site (`index.html`, CSS, or JS).
They were kept as local backups when the site's live images were migrated to
Cloudflare R2 (`https://media.fuheng-studio.com/static/…`).

The 28 images still in use live on R2 and are no longer stored in the repo's
top-level `images/` folder as the source of truth — the HTML points at R2.

Safe to delete if you don't need them; moved here on 2026-06-05 to declutter `images/`.
